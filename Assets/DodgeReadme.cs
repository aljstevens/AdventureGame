using UnityEngine;
using System.Collections;

public class DodgeReadme : MonoBehaviour {

	// Use this for initialization
	void Start () 
	{
	// This part rocks would be spawning at the top of the hill. They should be rolling down until they hit a collider where they would get destroyed. Or if they hit the player they damage them.
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
