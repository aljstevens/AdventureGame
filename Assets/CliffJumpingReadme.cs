using UnityEngine;
using System.Collections;

public class CliffJumpingReadme : MonoBehaviour {

	// Use this for initialization
	void Start () 
	{
	//Have the player need to jump onto these cliffs to get across. However after the player lands on the cliff, they get destroyed after 3-5 seconds or so. And if the player falls they fall into a crap load of fire.
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
